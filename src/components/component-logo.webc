<div class="hggsw-logo">
  <div class="hggs-logo"></div>
</div>

<style webc>
  @keyframes spin {
    100% {
      transform:rotate(360deg);
    }
  }

  .hggsw-logo .hggs-logo {
    width: var(--hggs-space-lg-default);
    height: var(--hggs-space-lg-default);
    animation: spin 8s linear infinite;
  }

  .hggsw-logo .hggs-logo:hover {
    animation-play-state: paused
  }
</style>
